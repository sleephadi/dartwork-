void main() {
  int x = 10;
  /*
  // postfixt,prefix
  x++; // postfixt increment (first it return then it increase)
  x--; // postfixt decrement (first it return then it decrease)
  ++x; // prefix increment (first it increase then it return)
  --x; // prefix decrement (first it decrease then it return)
  */
  ++x;
  --x;
  --x;
  print(x);
}
